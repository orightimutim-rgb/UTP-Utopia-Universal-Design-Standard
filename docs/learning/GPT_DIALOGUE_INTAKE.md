# 與 GPT 完整對話 — 學習入口

## 現況（2026-09-15）

Cursor Cloud Agent **無法直接讀取** ChatGPT App／網頁裡的私人對話。
已搜尋並確認目前沒有全文：

| 來源 | 結果 |
|------|------|
| Notion 知識庫 | 空（無列） |
| Gmail | 無 ChatGPT／OpenAI 對話匯出 |
| Cursor cloud agent transcripts | 僅有 Cursor 對話，不是 ChatGPT 匯出 |

## 請用下列任一方式提供全文

1. 把對話貼到 Notion「待學習：我與 GPT 的完整對話（intake）」頁  
2. 匯出後放到本倉庫（例如 `docs/learning/gpt-dialogue-export.md`）並開 PR／請 agent 讀  
3. 提供 ChatGPT 分享連結（若可公開讀取）

## 取得後我會學習並回寫的地方

- `AGENTS.md`／`docs/GITHUB_GPT_SYNC.md`（若對話含新規則）  
- `docs/TASK_BOARD.md`（若對話含優先順序）  
- `applications/ai-assistants/chatgpt-custom-instructions.md`（若對話含風格偏好）

## 已從 Cursor ↔ 你 的對話學到的（暫存）

1. 你要以 **iPhone + ChatGPT/Codex + GitHub** 為主工作面。  
2. **GitHub 能先做的先做**（文件、範本、PR、任務板），不要空等權限。  
3. 同步目標是讓 GPT／Cursor／GitHub **同一套安排**，避免各說各話。  
4. Issues 目前關閉；開啟前用 `TASK_BOARD.md` 當暫代真相來源。  
5. 合併小 PR、可回顧、可回退；不在未授權時 merge（本次你已明確授權依序完成含合併）。  
